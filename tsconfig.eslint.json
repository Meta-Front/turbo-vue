{
  // extend your base config to share compilerOptions, etc
  "extends": "@dow/tsconfig/vite.json",
  "compilerOptions": {
    // ensure that nobody can accidentally use this config for a build
    "noEmit": true
  },
  "include": [
    // whatever paths you intend to lint
    "apps",
    "configs",
    "packages",
    "scripts"
  ]
}
